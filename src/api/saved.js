export const addToSavedApi = (skuId) => (
  new Promise((resolve, reject) => {
    setTimeout(function (resp) {
      resolve(resp);
    }, 300);
  })
);

export const removeFromSavedApi = (skuId) => (
  new Promise((resolve, reject) => {
    setTimeout(function (resp) {
      resolve(resp);
    }, 300);
  })
);