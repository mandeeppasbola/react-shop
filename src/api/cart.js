export const addToCartApi = (skuId) => (
  new Promise((resolve, reject) => {
    setTimeout(function (resp) {
      resolve(resp);
    }, 300);
  })
);

export const removeFromCartApi = (skuId) => (
  new Promise((resolve, reject) => {
    setTimeout(function (resp) {
      resolve(resp);
    }, 300);
  })
);