export const addToFavApi = (skuId) => (
  new Promise((resolve, reject) => {
    setTimeout(function (resp) {
      resolve(resp);
    }, 300);
  })
);

export const removeFromFavApi = (skuId) => (
  new Promise((resolve, reject) => {
    setTimeout(function (resp) {
      resolve(resp);
    }, 300);
  })
);